def convert_to_int(el):
    return int(el)


nums = ["1", "2", "3", "4", "5"]

# numbers = [int(el) for el in nums]
# numbers = [convert_to_int(el) for el in nums]
# numbers = list(map(int, nums))
# numbers = list(map(lambda el: convert_to_int(el), nums))
# evens = [el for el in nums if el % 2 == 0]
# evens = list(filter(lambda x: x % 2 == 0, nums))


